export const PRIORITIES_COLORS: PRIORITIES_COLORS = {
  high: "red",
  medium: "yellow",
  low: "lime",
};

interface PRIORITIES_COLORS {
  high: string;
  medium: string;
  low: string;
  [key: string]: string;
}
